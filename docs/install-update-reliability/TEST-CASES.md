# 测试用例 — 安装/更新/Gateway重启 可靠性加固

> **版本**: v1.0  
> **日期**: 2026-03-09  
> **关联**: [DESIGN.md](./DESIGN.md) | [DFMEA.md](./DFMEA.md)

---

## 测试环境

- **容器**: 192.168.31.107:2223 (SSH user: wm_20)
- **Gateway 端口**: 18789
- **Web 面板端口**: 3000 (通过 Caddy 代理)
- **OpenClaw 版本**: 2026.3.8+

---

## TC-C1: npm 安装策略 — backup-then-install-in-place

### TC-C1-1: 正常安装流程（旧版本存在时）

| 项目 | 内容 |
|------|------|
| **前置条件** | openclaw 已安装，`openclaw -v` 可执行 |
| **操作步骤** | 1. 通过 Web 面板点击"更新"<br>2. 观察安装日志 |
| **预期结果** | 1. 日志显示"旧版本已备份到 /root/.npm-global-backup"<br>2. `npm install -g` 直接安装到正式 prefix<br>3. 日志显示验证通过<br>4. 日志显示"清理备份"<br>5. `openclaw -v` 输出正确版本<br>6. Gateway 自动重启成功 |
| **验证命令** | `ssh ... "openclaw -v && ls /root/.npm-global/lib/node_modules/openclaw/package.json"` |
| **DFMEA 覆盖** | N4, N2, N1 |

### TC-C1-2: 安装失败后旧版本自动还原

| 项目 | 内容 |
|------|------|
| **前置条件** | openclaw 已安装（如 2026.3.8）|
| **操作步骤** | 1. 模拟安装失败（可临时断网或 `iptables -A OUTPUT -d registry.npmjs.org -j DROP`）<br>2. 触发更新<br>3. 等待安装超时/失败 |
| **预期结果** | 1. 日志显示"安装失败，还原旧版本..."<br>2. `openclaw -v` 仍输出 2026.3.8<br>3. Gateway 仍可正常运行（或可以手动重启） |
| **验证命令** | `ssh ... "openclaw -v"` 应返回旧版本 |
| **DFMEA 覆盖** | N1 |

### TC-C1-3: 首次安装（无旧版本）

| 项目 | 内容 |
|------|------|
| **前置条件** | openclaw 未安装（`npm uninstall -g openclaw` 清理）|
| **操作步骤** | 1. 通过 Web 面板点击"安装" |
| **预期结果** | 1. 日志不显示"已备份"（无旧版本）<br>2. 直接安装<br>3. 安装完成后 `openclaw -v` 可用 |
| **DFMEA 覆盖** | N1 |

### TC-C1-4: 版本对齐场景（同版本不重复安装）

| 项目 | 内容 |
|------|------|
| **前置条件** | openclaw 已安装且为最新版本 |
| **操作步骤** | 1. 触发更新 |
| **预期结果** | 1. 日志显示"当前运行版本已满足目标版本，跳过安装"<br>2. 不执行 npm install |
| **DFMEA 覆盖** | N3 (间接) |

---

## TC-C2: npm view 失败处理

### TC-C2-1: npm view 双源均失败

| 项目 | 内容 |
|------|------|
| **前置条件** | 网络正常但 npm registry 不可达（模拟：`npm config set registry http://127.0.0.1:1`）|
| **操作步骤** | 1. 触发安装 |
| **预期结果** | 1. 日志显示 `[openclaw][warn] npm view 查询失败（两个源均不可用），跳过版本对齐检查`<br>2. 安装继续进行（不因版本查询失败而中断）<br>3. 不执行版本对齐的二次安装 |
| **验证方式** | 检查安装日志中是否有 warn 标记 |
| **DFMEA 覆盖** | N3 |

### TC-C2-2: 仅 npmmirror 可用

| 项目 | 内容 |
|------|------|
| **前置条件** | npmjs.org 不可达但 npmmirror 可达 |
| **操作步骤** | 1. 触发安装 |
| **预期结果** | 1. MIRROR_LATEST 有值，NPMJS_LATEST 为空<br>2. 不触发对齐（无法判断是否落后）<br>3. 安装使用 npmmirror 源 |
| **DFMEA 覆盖** | N3 |

---

## TC-C3: 安装后 watchdog 存活保障

### TC-C3-1: watchdog 存活时的正常流程

| 项目 | 内容 |
|------|------|
| **前置条件** | watchdog 正常运行（`pgrep -f openclaw-gateway-watchdog`）|
| **操作步骤** | 1. 触发安装/更新 |
| **预期结果** | 1. 安装日志显示"watchdog 存活确认"<br>2. Gateway 重启正常 |
| **DFMEA 覆盖** | G1 |

### TC-C3-2: watchdog 已死时的恢复

| 项目 | 内容 |
|------|------|
| **前置条件** | 手动杀死 watchdog: `pkill -f openclaw-gateway-watchdog`|
| **操作步骤** | 1. 确认 watchdog 已死<br>2. 触发安装/更新<br>3. 等待安装完成 |
| **预期结果** | 1. 安装完成后自动拉起 watchdog<br>2. 日志中不显示 watchdog 失败警告<br>3. Gateway 正常重启 |
| **验证命令** | `ssh ... "pgrep -f openclaw-gateway-watchdog"` 应返回 PID |
| **DFMEA 覆盖** | G1 |

---

## TC-C4: release 编译包校验

### TC-C4-1: 正常下载后的语法校验

| 项目 | 内容 |
|------|------|
| **前置条件** | 有可用的 release 编译包 |
| **操作步骤** | 1. 使用 release 模式安装 |
| **预期结果** | 1. 日志显示"校验 openclaw.mjs 语法完整性..."<br>2. 日志显示"openclaw.mjs 语法校验通过" |
| **DFMEA 覆盖** | R1 |

### TC-C4-2: 损坏文件的检测

| 项目 | 内容 |
|------|------|
| **前置条件** | 模拟损坏的 openclaw.mjs（手动替换为无效 JS）|
| **操作步骤** | 1. 下载编译包后，在校验前替换 openclaw.mjs 内容为 "CORRUPTED" |
| **预期结果** | 1. 日志显示"openclaw.mjs 语法校验失败（文件可能损坏）"<br>2. 安装失败退出码 14<br>3. 如果是 auto 模式，回退到源码构建 |
| **DFMEA 覆盖** | R1 |

---

## TC-C6: operation.lock 原子写入

### TC-C6-1: 正常写入和读取

| 项目 | 内容 |
|------|------|
| **前置条件** | 系统空闲 |
| **操作步骤** | 1. 触发安装<br>2. 检查 operation.lock 内容 |
| **预期结果** | 1. lock 文件是合法 JSON<br>2. 包含 type、taskId、startedAt、pid 字段 |
| **验证命令** | `ssh ... "cat /root/.openclaw/locks/operation.lock \| python3 -m json.tool"` |
| **DFMEA 覆盖** | O2 |

### TC-C6-2: 并发写入不损坏

| 项目 | 内容 |
|------|------|
| **前置条件** | 安装任务进行中 |
| **操作步骤** | 1. 安装过程中多次读取 operation.lock<br>2. 检查每次读取的内容 |
| **预期结果** | 每次读取的结果要么是完整的 JSON 要么文件不存在，不会出现半写入内容 |
| **DFMEA 覆盖** | O2 |

---

## TC-C7: orphan 子进程检测

### TC-C7-1: server.js 重启后检测到 orphan 进程

| 项目 | 内容 |
|------|------|
| **前置条件** | 安装任务进行中 |
| **操作步骤** | 1. 触发安装<br>2. 安装过程中重启 web server（kill + restart）<br>3. 检查 server.js 启动日志 |
| **预期结果** | 1. 启动日志显示 `[openclaw][orphan] Detected orphan install process pid=XXX`<br>2. 不主动杀死 orphan 进程<br>3. orphan 进程自然结束后 PID 文件被清理 |
| **验证命令** | `ssh ... "cat /root/.openclaw/locks/install-task.pid"` |
| **DFMEA 覆盖** | T2 |

### TC-C7-2: 正常安装后 PID 文件被清理

| 项目 | 内容 |
|------|------|
| **前置条件** | 无 |
| **操作步骤** | 1. 触发安装并等待完成 |
| **预期结果** | 安装完成后 `/root/.openclaw/locks/install-task.pid` 不存在 |
| **DFMEA 覆盖** | T2 |

---

## TC-C8: 前端轮询指数退避

### TC-C8-1: 短暂网络抖动后自动恢复

| 项目 | 内容 |
|------|------|
| **前置条件** | 安装任务进行中 |
| **操作步骤** | 1. 安装开始后，使用浏览器开发者工具模拟 offline 5 秒<br>2. 恢复网络 |
| **预期结果** | 1. 前端日志区域短暂停止更新<br>2. 网络恢复后自动继续更新<br>3. 不出现"轮询中断"错误<br>4. 控制台可见退避间隔增大日志 |
| **DFMEA 覆盖** | F2 |

### TC-C8-2: 持续断网 120 秒后放弃

| 项目 | 内容 |
|------|------|
| **前置条件** | 安装任务进行中 |
| **操作步骤** | 1. 安装开始后，持续断网超过 120 秒 |
| **预期结果** | 1. 前端显示"轮询中断"<br>2. 不是 ~5 秒就放弃（旧行为），而是尝试 120 秒 |
| **DFMEA 覆盖** | F2 |

### TC-C8-3: 正常安装全程无断网

| 项目 | 内容 |
|------|------|
| **前置条件** | 网络正常 |
| **操作步骤** | 1. 触发安装<br>2. 全程观察日志输出 |
| **预期结果** | 1. 日志实时更新无延迟<br>2. errorStreak 始终为 0<br>3. 安装完成后正常显示结果 |
| **DFMEA 覆盖** | F2 (回归) |

---

## 回归测试

### TC-REG-1: 完整安装+更新+重启流程

| 项目 | 内容 |
|------|------|
| **操作步骤** | 1. 卸载 openclaw<br>2. 通过面板安装<br>3. 等待 Gateway 启动<br>4. 通过面板更新<br>5. 等待 Gateway 重启<br>6. 手动点击"启动/重启" |
| **预期结果** | 所有步骤成功，Gateway /health 返回 200 |

### TC-REG-2: auto 模式三路径级联

| 项目 | 内容 |
|------|------|
| **操作步骤** | 1. 使用 auto 模式安装<br>2. 观察日志中尝试的路径 |
| **预期结果** | 1. 先尝试 npm 路径<br>2. npm 失败则尝试 release 路径<br>3. release 失败则尝试 source 路径<br>4. 某一路径成功后停止 |

### TC-REG-3: 卸载功能不受影响

| 项目 | 内容 |
|------|------|
| **操作步骤** | 1. 确认 openclaw 已安装<br>2. 通过面板卸载 |
| **预期结果** | 1. 卸载成功<br>2. `openclaw -v` 不可用<br>3. 5 分钟内不触发自动恢复 |

---

## 测试执行清单

| 用例 | 自动化 | 优先级 | 状态 |
|------|--------|--------|------|
| TC-C1-1 | 手动 | P0 | ⬜ 待测 |
| TC-C1-2 | 手动 | P0 | ⬜ 待测 |
| TC-C1-3 | 手动 | P0 | ⬜ 待测 |
| TC-C1-4 | 手动 | P1 | ⬜ 待测 |
| TC-C2-1 | 手动 | P0 | ⬜ 待测 |
| TC-C2-2 | 手动 | P1 | ⬜ 待测 |
| TC-C3-1 | 手动 | P1 | ⬜ 待测 |
| TC-C3-2 | 手动 | P0 | ⬜ 待测 |
| TC-C4-1 | 手动 | P1 | ⬜ 待测 |
| TC-C4-2 | 手动 | P1 | ⬜ 待测 |
| TC-C6-1 | 手动 | P1 | ⬜ 待测 |
| TC-C6-2 | 手动 | P1 | ⬜ 待测 |
| TC-C7-1 | 手动 | P1 | ⬜ 待测 |
| TC-C7-2 | 手动 | P1 | ⬜ 待测 |
| TC-C8-1 | 手动 | P0 | ⬜ 待测 |
| TC-C8-2 | 手动 | P1 | ⬜ 待测 |
| TC-C8-3 | 手动 | P0 | ⬜ 待测 |
| TC-REG-1 | 手动 | P0 | ⬜ 待测 |
| TC-REG-2 | 手动 | P1 | ⬜ 待测 |
| TC-REG-3 | 手动 | P1 | ⬜ 待测 |
